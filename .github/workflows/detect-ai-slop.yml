name: 🛡️ PR Quality Guard

on:
  pull_request_target:
    types:
      - opened
      - reopened
      - synchronize
      - ready_for_review

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: pr-quality-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  quality-check:
    name: Detect Low Quality PRs
    runs-on: ubuntu-latest

    if: |
      github.event.pull_request.draft == false &&
      github.actor != 'github-actions[bot]'

    steps:
      - name: Run Anti-Slop Detection
        uses: peakoss/anti-slop@v0.2.1
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}

          max-failures: 5

          require-description: true
          require-linked-issue: true
          require-pr-template: true
          require-maintainer-can-modify: true

          require-conventional-title: true
          require-conventional-commits: false

          max-description-length: 2200
          max-emoji-count: 5

          detect-spam-usernames: true
          min-account-age: 5

          blocked-terms: |
            generated by chatgpt
            generated with chatgpt
            fully generated by ai
            auto generated pull request
            this pr was automatically generated
            created entirely using ai
            no manual changes were made
            prompt used
            as an ai language model
            chatgpt said
            claude generated
            gemini wrote
            grok generated

          strict-pr-template-sections: |
            checklist
            description

          blocked-source-branches: |
            main
            master

          exempt-author-association: OWNER,MEMBER,COLLABORATOR

          exempt-bots: |
            github-actions[bot]
            dependabot[bot]

          failure-add-pr-labels: |
            suspected-ai-slop
            needs-human-review

          failure-pr-message: |
            ## ⚠️ PR Needs Maintainer Review

            This PR was flagged by automated quality checks.

            A maintainer will review it shortly.

            Common causes:
            - Missing linked issue
            - Incomplete PR template
            - Low-effort submission
            - Suspicious autogenerated content

          close-pr: false
          lock-pr: false